{% extends 'base.html.twig' %}

{# input: personaList - array of persona #}

{% block body %}

<table>
  <tr>
    <td width="200px">
      <center>
        <a href="{{ path('homepage') }}">
          <img src="{{ asset('assets/images/icon-home.svg') }}" 
	       alt="icon-home" height="96" width="96"></a>
      </center>
    </td>
  </tr>
  <tr>
    <td><center><a href="{{ path('homepage') }}">Pagina principale</a></center></td>
  </tr>
</table>

<table id="atable">
  <thead>
    <tr>
        <td>Id</td>
        <td>Nome</td>
        <td>Cognome</td>
        <td>Codice fiscale</td>
	<td>Codice CRI</td>
        <td>Altro</td>
    </tr>
  </thead>
  <tbody>
  {% for persona in personaList %}
    <tr>
        <td>{{ persona.id }}</td>
        <td>{{ persona.nome }}</td>
        <td>{{ persona.cognome }}</td>
        <td>{{ persona.codiceFiscale }}</td>
        <td>{{ persona.codiceCRI }}</td>
        <td>{{ persona.altro }}</td>
    </tr>
  {% endfor %}
  </tbody>
</table>

{% endblock %}
